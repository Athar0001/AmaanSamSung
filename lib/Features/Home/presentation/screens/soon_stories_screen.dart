import 'package:flutter/material.dart';

class SoonStoriesScreen extends StatelessWidget {
  const SoonStoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Soon Stories Screen Stub')),
    );
  }
}
